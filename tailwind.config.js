module.exports = {
  darkMode: 'class', // Enable dark mode using the 'class' strategy
  content: ['./index.html'], // Specify the files Tailwind should scan for class names
  theme: {
    extend: {},
  },
  plugins: [],
};